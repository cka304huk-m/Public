#Расчёт кубатуры вытяжки для кухни
a = 12
while True:
    print("Для завершения напишите - 'выход'")
    y = str(input('Известна кубатура кухни? Да или нет: '))
    if y == 'нет':
        try:
            dl = float(input('Длина: '))
            sh = float(input('Ширина: '))
            vi = float(input('Высота: '))
            rez = dl * sh * vi * a
        except ValueError:
            print('Нужно вводить только цифры! Давайте начнем с начала!')
        else:
            print('Необходимая кубатура:',+rez)
    else:
        try:
            r2_kv = float(input('Введите сколько квадратов:'))
            r2_vi = float(input('Введите высоту:'))
            r2_rez = r2_kv * r2_vi * a
        except ValueError:
            print('Нужно вводить только цифры! Давайте начнем с начала!')
        else:
            print('Необходимая кубатура:',+r2_rez)
